import React from 'react'
import classes from '../styles/common.module.scss'
import zombie from '../images/zombie.png'

export default () => {
  return <img className={classes.image} src={zombie} />
}
