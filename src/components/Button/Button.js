import styled from 'styled-components';

const Button = styled.button`
  border-radius: 5px;
  display: block;
  font-weight: 900;
  padding: 0.5rem;
  &:hover {
    cursor: pointer;
  }
`;

export default Button;
